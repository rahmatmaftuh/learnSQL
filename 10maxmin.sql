MAX & MIN = To find rows that have min or max value

MAX(column_name)
MIN(column_name)

example
SELECT MAX(price)
FROM purchases;

SELECT MIN(price)
FROM purchases;

SELECT MAX(price)
FROM purchases;
WHERE character_name = "Ken the Ninja"